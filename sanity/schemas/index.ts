import imageCard from "./image-card-schema";
import AboutInfo from "./info-about-schema";
import project from "./project-schema";

const schemas = [project, imageCard, AboutInfo];

export default schemas;
